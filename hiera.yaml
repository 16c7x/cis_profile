---
version: 5
lookup_options:
  cis_profile::exclude_rules:
    merge: deep

defaults:
  datadir: 'data'
  data_hash: 'yaml_data'
hierarchy:

  - name: 'node'
    path: "nodes/%{::trusted.certname}.yaml"

  - name: 'common'
    path: 'common.yaml'
